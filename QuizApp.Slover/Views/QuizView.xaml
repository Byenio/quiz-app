<Page x:Class="QuizApp.Slover.Views.QuizView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuizApp.Slover.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="QuizView">

    <Grid>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ListView x:Name="QuestionListView" ItemsSource="{Binding Questions}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock x:Name="txtQuestion" Text="{Binding Text}" FontSize="18"/>
                            <CheckBox x:Name="checkAnswer1" Content="{Binding Answers[0].Text}" FontSize="18" Margin="10"/>
                            <CheckBox x:Name="checkAnswer2" Content="{Binding Answers[1].Text}" FontSize="18" Margin="10"/>
                            <CheckBox x:Name="checkAnswer3" Content="{Binding Answers[2].Text}" FontSize="18" Margin="10"/>
                            <CheckBox x:Name="checkAnswer4" Content="{Binding Answers[3].Text}" FontSize="18" Margin="10"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </ScrollViewer>
        
    </Grid>
</Page>
